# Web framework
flask

# Translation (Python 3.13 compatible)
deep-translator

# Speech-to-text (Google Cloud STT — lightweight, no model download)
SpeechRecognition

# Optional: bundles ffmpeg automatically if not installed system-wide
# Uncomment if ffmpeg is not available on your system:
# imageio-ffmpeg

# Text-to-speech
edge-tts

gunicorn
# Audio conversion is done via direct ffmpeg subprocess (no Python lib needed)

# HTTP client (used for ElevenLabs TTS API)
requests

# OpenAI agent (multilanguage refinement)
openai>=1.0.0

# Load secrets from .env
python-dotenv
